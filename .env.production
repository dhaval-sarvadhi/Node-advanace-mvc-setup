# Server
PORT=8080
NODE_ENV=production

# Database (example: production DB)
DB_HOST=prod-db-host.example.com
DB_PORT=3306
DB_NAME=myapp_prod
DB_USER=prod_user
DB_PASSWORD=your_prod_password

# JWT Secret (use a strong secret in prod!)
JWT_SECRET=your_production_jwt_secret

# Other production-specific configs...
